{
  "routes": [
    {
      "method": "GET",
      "path": "/check",
      "handler": "bullmq-queue.check",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/publish",
      "handler": "bullmq-queue.publish",
      "config": {
        "policies": []
      }
    }
  ]
}
